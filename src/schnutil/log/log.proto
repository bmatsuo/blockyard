message LogEvent {
    // metadata
    required string source = 1; // pretty general...
    required string timestamp = 2;
    repeated string tags = 5;

    // content
    required string message = 8;
    // TODO named values
}
